- hosts: localhost
  connection: local
  become: false
  tasks:
    - name: Run kong sync tasks
      include_role:
        name: kong
        tasks_from: deck_sync
  vars_files:
    - ../../group_vars/nonprod.yml